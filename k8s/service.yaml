# Deploy after API is ready, alongside deployment.yaml
apiVersion: v1
kind: Service
metadata:
  name: neuro-sim-svc
  namespace: neuro-sim
spec:
  selector:
    app: neuro-sim
  ports:
    - port: 80
      targetPort: 8000
  type: LoadBalancer
